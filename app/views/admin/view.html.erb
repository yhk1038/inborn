<div class="">
  <div class="col-md-6 col-sm-6 col-xs-12">
    <div class="x_panel">
      <div class="x_title">
        <h2><i class="fa fa-bars"></i> 메인화면 좌측 상단 게시판 <small></small></h2>
        <ul class="nav navbar-right panel_toolbox">
          <li><a class="collapse-link"><i class="fa fa-chevron-up"></i></a>
          </li>
          <li class="dropdown">
            <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-expanded="false"><i class="fa fa-wrench"></i></a>
            <ul class="dropdown-menu" role="menu">
              <li><a href="#">Settings 1</a>
              </li>
              <li><a href="#">Settings 2</a>
              </li>
            </ul>
          </li>
          <li><a class="close-link"><i class="fa fa-close"></i></a>
          </li>
        </ul>
        <div class="clearfix"></div>
      </div>
      <div class="x_content">

        <% m = MainBoard.where(board_type: 1).first %>
        <% m = MainBoard.new if m.nil? %>
        <form action="/admin/view" method="POST" data-parsley-validate class="form-horizontal form-label-left">
          <input type="hidden" value="1" name="board_type" />
          <div class="form-group">
            <label class="control-label col-md-3 col-sm-3 col-xs-12">연결된 게시판</label>
            <div class="col-md-6 col-sm-6 col-xs-12">
              <select name="board_id" class="form-control col-md-7 col-xs-12">
                <option value="">선택</option>
                <% Board.all.each do |b| %>
                  <option value="<%= b.id %>" <%= 'selected' if !(m.board.nil?) && m.board.id == b.id %>><%= b.title %></option>
                <% end %>
              </select>
            </div>
            <div class="ln_solid"></div>
            <div class="form-group">
              <div class="col-md-6 col-sm-6 col-xs-12 col-md-offset-3">
                <button type="submit" class="btn btn-success">확인</button>
              </div>
            </div>
          </div>

        </form>

      </div>
    </div>
  </div>


  <div class="">
    <div class="col-md-6 col-sm-6 col-xs-12">
      <div class="x_panel">
        <div class="x_title">
          <h2><i class="fa fa-bars"></i> 메인화면 우측 상단 게시판 <small></small></h2>
          <ul class="nav navbar-right panel_toolbox">
            <li><a class="collapse-link"><i class="fa fa-chevron-up"></i></a>
            </li>
            <li class="dropdown">
              <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-expanded="false"><i class="fa fa-wrench"></i></a>
              <ul class="dropdown-menu" role="menu">
                <li><a href="#">Settings 1</a>
                </li>
                <li><a href="#">Settings 2</a>
                </li>
              </ul>
            </li>
            <li><a class="close-link"><i class="fa fa-close"></i></a>
            </li>
          </ul>
          <div class="clearfix"></div>
        </div>
        <div class="x_content">

          <% m = MainBoard.where(board_type: 2).first %>
          <% m = MainBoard.new if m.nil? %>
          <form action="/admin/view" method="POST" data-parsley-validate class="form-horizontal form-label-left">
            <input type="hidden" value="2" name="board_type" />
            <div class="form-group">
              <label class="control-label col-md-3 col-sm-3 col-xs-12">연결된 게시판</label>
              <div class="col-md-6 col-sm-6 col-xs-12">
                <select name="board_id" class="form-control col-md-7 col-xs-12">
                  <option value="">선택</option>
                  <% Board.all.each do |b| %>
                    <option value="<%= b.id %>" <%= 'selected' if !(m.board.nil?) && m.board.id == b.id %>><%= b.title %></option>
                  <% end %>
                </select>
              </div>
              <div class="ln_solid"></div>
              <div class="form-group">
                <div class="col-md-6 col-sm-6 col-xs-12 col-md-offset-3">
                  <button type="submit" class="btn btn-success">확인</button>
                </div>
              </div>
            </div>

          </form>

        </div>
      </div>
    </div>

    <div class="">
      <div class="col-md-12 col-sm-12 col-xs-12">
        <div class="x_panel">
          <div class="x_title">
            <h2><i class="fa fa-bars"></i> 메인화면 하단 게시판 <small></small></h2>
            <ul class="nav navbar-right panel_toolbox">
              <li><a class="collapse-link"><i class="fa fa-chevron-up"></i></a>
              </li>
              <li class="dropdown">
                <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-expanded="false"><i class="fa fa-wrench"></i></a>
                <ul class="dropdown-menu" role="menu">
                  <li><a href="#">Settings 1</a>
                  </li>
                  <li><a href="#">Settings 2</a>
                  </li>
                </ul>
              </li>
              <li><a class="close-link"><i class="fa fa-close"></i></a>
              </li>
            </ul>
            <div class="clearfix"></div>
          </div>
          <div class="x_content">


            <div class="" role="tabpanel" data-example-id="togglable-tabs">
              <ul id="myTab" class="nav nav-tabs bar_tabs" role="tablist">
                <li role="presentation" class="active"><a href="#tab_content1" id="home-tab" role="tab" data-toggle="tab" aria-expanded="true">탭 생성</a>
                </li>
                <% i = 2 %>
                <% MainBoard.where(board_type: 3).each do |m| %>
                  <li role="presentation" class=""><a href="#tab_content<%= i %>" role="tab" data-toggle="tab" aria-expanded="false"><%= m.board.title %></a>
                  </li>
                <%
                  i += 1
                end
                %>

              </ul>
              <div id="myTabContent" class="tab-content">
                <div role="tabpanel" class="tab-pane fade active in" id="tab_content1" aria-labelledby="home-tab">
                  <form action="/admin/view" method="POST" data-parsley-validate class="form-horizontal form-label-left">
                    <input type="hidden" name="board_type" value="3">
                    <div class="form-group">
                      <label class="control-label col-md-3 col-sm-3 col-xs-12">새로 연결할 게시판</label>
                      <div class="col-md-6 col-sm-6 col-xs-12">
                        <select name="board_id" class="form-control col-md-7 col-xs-12">
                          <option value="">선택</option>
                          <% Board.all.each do |b| %>
                            <option value="<%= b.id %>" <%= 'selected' if !(m.board.nil?) && m.board.id == b.id %>><%= b.title %></option>
                          <% end %>
                        </select>
                      </div>
                    </div>
                    <div class="ln_solid"></div>
                    <div class="form-group">
                      <div class="col-md-6 col-sm-6 col-xs-12 col-md-offset-3">
                        <button type="submit" class="btn btn-success">확인</button>
                      </div>
                    </div>
                  </form>
                </div>

                <% i = 2 %>
                <% MainBoard.where(board_type: 3).each do |m| %>
                  <div role="tabpanel" class="tab-pane fade" id="tab_content<%= i %>" aria-labelledby="home-tab">
                    <form action="/admin/view/<%= m.id %>" method="POST" data-parsley-validate class="form-horizontal form-label-left">
                      <input type="hidden" value="3" name="board_type" />
                      <div class="form-group">
                        <label class="control-label col-md-3 col-sm-3 col-xs-12">연결된 게시판</label>
                        <div class="col-md-6 col-sm-6 col-xs-12">
                          <select name="board_id" class="form-control col-md-7 col-xs-12">
                            <option value="">선택</option>
                            <% Board.all.each do |b| %>
                              <option value="<%= b.id %>" <%= 'selected' if !(m.board.nil?) && m.board.id == b.id %>><%= b.title %></option>
                            <% end %>
                          </select>
                        </div>
                      </div>


                      <div class="ln_solid"></div>
                      <div class="form-group">
                        <div class="col-md-6 col-sm-6 col-xs-12 col-md-offset-3">
                          <button type="submit" class="btn btn-success">확인</button>
                          <%= link_to '삭제',  "/admin/view/delete/#{m.id}", class: 'btn btn-danger',
                          data: { confirm: '정말 삭제하시겠습니까?' } %>
                        </div>
                      </div>
                    </form>
                  </div>
                <%
                i += 1
                end
                %>

              </div>
            </div>

          </div>
        </div>
      </div>
